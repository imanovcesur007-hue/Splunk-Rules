index=main _index_earliest=-1m
| eval payload_decoded=urldecode(_raw)
| where match(payload_decoded, "(?i)(?:\.\.[\\/\\\\]|/etc/(?:shadow|hosts|passwd)|c:\\\\windows\\\\|boot\.ini|/proc/self/)") AND NOT match(payload_decoded, "(?i)(\.log|\/var\/log|\.env)")
| table _time, _raw
