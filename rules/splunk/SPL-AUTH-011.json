{
  "rule_id": "SPL-AUTH-011",
  "rule_name": "Targeted Account Brute Force Detection",
  "description": "Detects 5 or more failed login attempts targeting a single user account from the same IP address within a 1-minute window, indicating an active brute-force attack.",
  "severity": "High",
  "owasp_category": "Authentication/Brute Force",
  "search_query": "index=main sourcetype=_json action=login_failed earliest=-1m | stats count by src_ip, user | where count >= 5"
}
