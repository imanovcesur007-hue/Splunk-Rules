{
  "rule_id": "SPL-OWASP-A04",
  "rule_name": "Business Logic Workflow Bypass Detection",
  "description": "Detects instances where a user reaches the final stages of a critical transaction sequence without executing the required preceding authentication or payment steps.",
  "severity": "High",
  "owasp_category": "A04:2021-Insecure Design",
  "search_query": "index=web sourcetype=web_app_logs (action=\"checkout_complete\" OR action=\"payment_start\") | transaction session_id maxpause=5m | where NOT match(action, \"payment_success\") AND match(action, \"checkout_complete\")"
}
