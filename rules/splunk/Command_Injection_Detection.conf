index=main sourcetype=_json _index_earliest=-1m
| eval decoded_cmd=urldecode(_raw)
| where match(decoded_cmd, "(?i)(?:[;\|]|&&|\$\()\s*(cat|ls|whoami|id|uname|wget|curl|nc|bash|ping|netstat)\b")
| table _time, _raw
